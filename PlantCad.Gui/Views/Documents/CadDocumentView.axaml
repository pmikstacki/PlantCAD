<UserControl xmlns="https://github.com/avaloniaui"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:vm="using:PlantCad.Gui.ViewModels.Documents"
             xmlns:controls="using:PlantCad.Gui.Controls"
             x:Class="PlantCad.Gui.Views.Documents.CadDocumentView"
             x:DataType="vm:CadDocumentViewModel">
  <controls:CadViewportControl x:Name="Viewport" Model="{Binding Model}" RenderOptions="{Binding RenderOptions}" IsSelecting="{Binding IsSelecting}" SelectedEntity="{Binding SelectedEntity}" />
</UserControl>
